# 🚀 快速開始指南

## 前置準備

### 1. 確認 LM Studio 運行中

1. 開啟 LM Studio
2. 載入模型（你提到的 `gemma-3n-e4b-it-mlx`）
3. 點擊左側 "Local Server" 或 "Developer" 標籤
4. 點擊綠色 "Start Server" 按鈕
5. 確認顯示 `Server running on http://localhost:1234`

### 2. 測試 LM Studio API

```bash
curl http://localhost:1234/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gemma-3n-e4b-it-mlx",
    "messages": [{"role": "user", "content": "Hello"}],
    "temperature": 0.7,
    "stream": false
  }'
```

如果看到 JSON 回應，表示成功！

## 啟動 Agentic AI

### 方式一：使用啟動腳本（推薦）

```bash
cd /Users/40gpu/coding_projects/langgraph-local
./start.sh
```

### 方式二：直接執行

```bash
python3 chat_client.py
```

## 測試 Agentic 行為

### 測試 1：檔案系統自主操作

```
💬 你: 列出當前目錄的所有檔案

預期行為:
- Agent 自動呼叫 list_directory 工具
- 自動分析結果
- 給出完整列表
```

### 測試 2：多步驟任務

```
💬 你: 建立一個叫 test.txt 的檔案，內容寫 "Hello Agentic AI"，然後讀取確認

預期行為:
- Agent 自動執行 create_file
- 自動執行 read_file 驗證
- 回報結果
```

### 測試 3：分析任務（真正的 Agentic）

```
💬 你: 分析這個專案的 Python 檔案，告訴我每個檔案的功能

預期行為:
- 自動列出目錄
- 自動過濾 .py 檔案
- 自動讀取每個檔案
- 自動分析內容
- 給出完整報告

這就是 Agentic AI！你只給意圖，它自己完成所有步驟。
```

## 與傳統 Chatbot 的差異

### ❌ 傳統 Chatbot

```
你: "分析 Python 檔案"
Bot: "請問您要分析哪個檔案？"
你: "agent.py"
Bot: "好的，請問您要分析什麼方面？"
你: "它的功能"
Bot: "..."
```

### ✅ Agentic AI (本專案)

```
你: "分析這個專案的 Python 檔案"
Agent:
  [自動] 列出目錄 → 找到 agent.py, chat_client.py
  [自動] 讀取 agent.py → 分析內容
  [自動] 讀取 chat_client.py → 分析內容
  [回報] "專案包含 2 個主要檔案:
         - agent.py: 實作 ReAct Agent...
         - chat_client.py: 提供 Terminal 介面..."
```

## 可用指令

在對話中輸入：

- `/help` - 顯示幫助
- `/info` - 查看當前狀態
- `/clear` - 清除對話記憶
- `/exit` - 離開

## 常見問題

### Q: Agent 不夠 "agentic"，一直問我問題？

A: 可能原因：
1. **模型太小** - 建議至少 4B 參數的模型
2. **Temperature 太高** - 在 `agent.py` 中調低 temperature
3. **Prompt 需要調整** - 修改 `agent.py` 中的 `state_modifier`

### Q: 連接失敗

A: 檢查清單：
1. LM Studio 是否運行？
2. Server 是否啟動（綠色按鈕）？
3. Port 是 1234 嗎？
4. 模型名稱對嗎？（在 `agent.py` 中檢查）

### Q: MCP 工具無法使用

A: 確認 Node.js 已安裝：
```bash
node --version  # 需要 v16+
```

## 架構圖

```
┌─────────────────────────────────────────┐
│  Terminal Client (chat_client.py)       │
│  - 多輪對話                              │
│  - 指令處理                              │
└─────────────┬───────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  LangGraph ReAct Agent (agent.py)       │
│  ┌───────────────────────────────────┐  │
│  │  推理 (Reasoning)                 │  │
│  │    ↓                              │  │
│  │  行動 (Action - 呼叫工具)        │  │
│  │    ↓                              │  │
│  │  觀察 (Observation - 工具結果)   │  │
│  │    ↓                              │  │
│  │  (循環直到完成)                   │  │
│  └───────────────────────────────────┘  │
└─────────────┬───────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  MCP Client (langchain-mcp-adapters)    │
│  - 轉換 MCP 工具為 LangChain Tools      │
└─────────────┬───────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  MCP Servers (透過 npx)                 │
│  - filesystem: 檔案操作                 │
│  - (可擴充) bash: 執行指令              │
│  - (可擴充) ripgrep: 搜尋程式碼         │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  LM Studio (localhost:1234)             │
│  - gemma-3n-e4b-it-mlx                  │
│  - OpenAI Compatible API                │
└─────────────────────────────────────────┘
```

## 下一步

1. **擴充工具** - 在 `agent.py` 加入 bash、ripgrep MCP servers
2. **優化 Prompt** - 調整 `state_modifier` 讓 Agent 更聰明
3. **加入持久化** - 使用 LangGraph 的 checkpoint 功能保存對話
4. **Web UI** - 建立 Streamlit 或 Gradio 介面

享受你的 Agentic AI！🎉
